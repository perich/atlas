{
  "name": "@bankops/analyst-model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "NODE_OPTIONS=--conditions=development vitest src",
    "test:run": "NODE_OPTIONS=--conditions=development vitest run src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bankops/contracts": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "4.1.5"
  }
}
